<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Management</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>Account Management</h1>
    </header>
    <main>
        <% if (user) { %>
            <h2>Welcome, <%= user.account_email %></h2>
            <p>Account Type: <%= user.account_type %></p>
        
            <% if (user.account_type === 'Employee' || user.account_type === 'Admin') { %>
                <!-- Employee/Admin specific content -->
            <% } %>
        <% } else { %>
            <h2>Welcome, Guest</h2>
            <p>Please <a href="/account/login">login</a> to access your account.</p>
        <% } %>


        <p>Your JWT token is stored in a cookie.</p>
    </main>
    <footer>
        <p>&copy; 2025 CSE Motors</p>
    </footer>
</body>
</html>
